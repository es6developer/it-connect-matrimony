apiVersion: v1
kind: Service
metadata:
  name: grafana
  namespace: it-connect
  labels:
    app: grafana
    component: monitoring
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 3000
      targetPort: 3000
      protocol: TCP
  selector:
    app: grafana
