apiVersion: v1
kind: Service
metadata:
  name: web
  namespace: it-connect
  labels:
    app: web
    component: frontend
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 3000
      targetPort: 3000
      protocol: TCP
  selector:
    app: web
